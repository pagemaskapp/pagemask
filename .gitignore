# dependências
node_modules/
.pnp
.pnp.*

# build
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# ambiente — o único que entra no git é o modelo
.env
.env.*
!.env.example

# vercel
.vercel/

# dumps do banco (scripts/backup-diario.sh). NAO e "arquivo grande demais":
# um dump tem token do Instagram cifrado, e-mail de todo cliente e a trilha de
# auditoria inteira. Commitar um e um vazamento que o historico do git guarda
# para sempre — e que `git rm` no dia seguinte nao desfaz.
backups/
*.dump

# pastas de trabalho do pipeline — video bruto e video renderizado.
# CLAUDE.md: midia pesada fica FORA do git, no Drive. Historico de git nunca
# encolhe: um MP4 commitado uma vez pesa em todo clone para sempre, mesmo
# depois de apagado. O `video-pipeline` copiado na Fase 3 chega com exemplos
# reais dentro dessas pastas.
worker/input/
worker/output/
worker/work/
worker/reports/

# python (worker)
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# mídia: entrada e saída de render não são versionadas
*.mp4
*.mov
*.mkv
*.webm

# sistema
.DS_Store
Thumbs.db
*.pem
npm-debug.log*
yarn-error.log*
coverage/

.vercel
